<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
    <h2>Cadastrar turma</h2>
    </header>
    <main>
        <div class="card">
            <form action="/turma" method="POST">
                <label for="nome">Nome da turma:</label>
                <input type="text" id="nome" name="nome" placeholder="Nome da turma" required>

                <label for="tutorId">Tutor:</label>
                <select name="tutorId" id="tutorId" required>
                    {{#each tutores}}
                        <option value="{{id}}">{{nome}}</option>
                    {{/each}}
                </select>

                <label for="aulaId">Aula:</label>
                <select name="aulaId" id="aulaId" required> 
                    {{#each aulas}}
                        <option value="{{id}}">{{nome}}</option>
                    {{/each}}
                </select>

                <label for="pessoaIds">Pessoa:</label>
                <select name="pessoaIds[]" id="pessoaIds" multiple required>
                    {{#each pessoas}}
                        <option value="{{id}}">{{pessoa}}</option>
                    {{/each}}
                </select>

                <button type="submit">Salvar</button>
            </form>

            <a class="btn" href="/homeTurma">Voltar</a>
        </div>
    </main>
</body>
</html>