<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
    <h2>Editar Turma</h2>
    </header>
    <main>
    <div class="card"> 
    <form action="/turma/{{turma.id}}/editar" method="post">
        <h3> alunos </h3>
        <input type="text" name="alunos" value="{{turma.alunos}}" required>
        <h3> tutor </h3>
        <input type="text" name="tutor" value="{{turma.tutor}}" required>
        <h3> aula </h3>
        <input type="text" name="aula" value="{{turma.aula}}" required>
        <button type="submit">Salvar</button>
    </form>
    <a class="btn" href="/turma">Voltar</a>
    </div>
    </main>
</body>
</html>