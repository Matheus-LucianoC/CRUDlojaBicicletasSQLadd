<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
    <h2>Editar Plano</h2>
    </header>
    <main>
    <div class="card"> 
    <form action="/planos/{{plano.id}}/editar" method="post">
        <h3> nome </h3>
        <input type="text" name="nome" value="{{plano.nome}}" required>
        <h3> descrição </h3>
        <input type="text" name="beneficios" value="{{plano.beneficios}}" required>
        <h3> preço </h3>
        <input type="number" name="preco" value="{{plano.preco}}" required>
        <h3> horas_semanais </h3>
        <input type="number" name="horas_limite" value="{{plano.horas_limite}}" required>
        <button type="submit">Salvar</button>
    </form>
    <a class="btn" href="/planos">Voltar</a>
    </div>
    </main>
</body>
</html>